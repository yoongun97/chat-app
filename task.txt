## EPIC 1. 프로젝트 세팅 및 환경 구성

### Task 1.1 Next.js 프로젝트 초기화

- [ ]  `npx create-next-app@latest`
- [ ]  TypeScript 활성화
- [ ]  폴더 구조 정리 (e.g., `/components`, `/lib`, `/features`, `/app` 등)

### Task 1.2 TailwindCSS & shadcn/ui 세팅

- [ ]  Tailwind 설치 및 `tailwind.config.ts` 설정
- [ ]  shadcn/ui 설치 및 초기 컴포넌트 생성 (`npx shadcn-ui@latest init`)

### Task 1.3 Supabase 연동

- [ ]  Supabase 프로젝트 생성 및 API 키 설정 (`.env`)
- [ ]  Supabase client 유틸 작성 (`lib/supabase.ts`)
- [ ]  Supabase Auth 및 Database 연결 확인

---

## EPIC 2. 인증 (Auth) 시스템

### Task 2.1 로그인/회원가입 페이지 구현

- [ ]  `@supabase/auth-helpers-nextjs`로 OAuth/Auth 기능 통합
- [ ]  이메일 + 비밀번호 로그인/회원가입 폼 제작 (shadcn Form 컴포넌트)
- [ ]  폼 제출 시 에러/성공 핸들링 + 토스트 알림 적용

### Task 2.2 인증 상태 보호

- [ ]  서버사이드 `getServerSession` 혹은 미들웨어로 보호 라우팅 설정
- [ ]  인증된 유저만 채팅 접근 가능

---

## EPIC 3. 데이터베이스 설계 (Supabase)

### Task 3.1 테이블 구조 정의

- `users`: Supabase 기본 제공
- `chats`: 각 채팅방 (user_id, title, created_at)
- `messages`: 채팅 내 메시지 (chat_id, role, content, created_at)

### Task 3.2 RLS 정책 설정

- [ ]  로그인된 유저만 자신의 채팅방/메시지에 접근 가능하도록 RLS 구성

---

## EPIC 4. 채팅 UI 및 기능

### Task 4.1 채팅방 레이아웃

- [ ]  전체 화면 layout 구성 (Sidebar + Chat Area)
- [ ]  Sidebar에 채팅방 목록 출력 (최근순, 제목은 `첫 메시지` or 수동 입력)
- [ ]  선택된 채팅방 강조 및 전환 기능

### Task 4.2 채팅 기능 구현

- [ ]  채팅 메시지 입력 폼 (Enter로 전송, Loading 처리)
- [ ]  입력 시 Supabase에 메시지 저장 (`role: user`)
- [ ]  GPT API 요청 후 답변 저장 (`role: assistant`)
- [ ]  응답 처리 중 로딩 표시 (Skeleton, spinner 등)

---

## EPIC 5. GPT API 연동

### Task 5.1 OpenAI API 연동 준비

- [ ]  `.env.local`에 `OPENAI_API_KEY` 설정
- [ ]  `lib/openai.ts` 생성하여 API 호출 함수 작성

### Task 5.2 대화 흐름 구현

- [ ]  `chat_id` 기준으로 과거 메시지 fetch
- [ ]  `messages` 배열로 구성 후 GPT API에 전달
- [ ]  GPT 응답을 받아 DB에 저장 및 화면 업데이트

---

## EPIC 6. 채팅방 관리 기능

### Task 6.1 채팅방 생성/삭제

- [ ]  새 대화 버튼으로 `chats` 새 row 생성
- [ ]  삭제 버튼으로 채팅방 + 관련 메시지 soft delete 또는 hard delete

### Task 6.2 제목 수정 기능 (선택)

- [ ]  Chat title hover 시 수정 아이콘 노출
- [ ]  Modal or inline form으로 제목 변경

---

## EPIC 7. UI/UX 개선

### Task 7.1 컴포넌트 적용 (shadcn/ui)

- [ ]  Button, Input, Sheet, Dialog, Skeleton, Toast 활용
- [ ]  토스트 알림 추가 (성공, 실패 등 상황별)

### Task 7.2 반응형 대응

- [ ]  모바일 환경에서 sidebar 토글 (`<Sheet>` 활용)
- [ ]  채팅창 자동 스크롤, 입력창 고정 등 UX 최적화

---

## EPIC 8. 배포 및 마무리

### Task 8.1 배포

- [ ]  Vercel에 배포 (Supabase 연결 확인)
- [ ]  환경 변수 (`OPENAI_API_KEY`, `SUPABASE_URL`, `ANON_KEY`) 설정

### Task 8.2 최종 QA

- [ ]  로그인 흐름 검토
- [ ]  GPT 응답 속도 및 오류 처리 확인
- [ ]  비정상 시 fallback UI 및 에러 경고 확인

---

# 🧱 보너스: 확장 아이디어

- 다중 모델 선택 (GPT-3.5 / GPT-4)
- 유저별 메시지 token 사용량 표시
- 메시지 수정/재전송 기능
- 시스템 프롬프트 설정
- 다크모드 토글 (shadcn/ui는 기본 지원)